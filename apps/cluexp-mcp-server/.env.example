# Local/internal-preview only. Copy to your shell or local secret manager; do not commit real values.
#
# Use a local/dev API URL. Do not point this at https://intake.cluexp.com unless
# a separate production MCP preview has been explicitly authorized.
CLUEXP_API_BASE_URL=http://127.0.0.1:8000

# Use a non-production external API key with only the first-preview scopes:
# services:read, coverage:check, service_requests:write, service_requests:read
CLUEXP_API_KEY=replace-with-local-dev-api-key
