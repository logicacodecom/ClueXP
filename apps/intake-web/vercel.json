{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "functions": {
    "api/**/*.py": {
      "excludeFiles": "{node_modules/**,.next/**,**/*.png,**/*.zip}"
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/main" }
  ]
}
